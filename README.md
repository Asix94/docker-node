# Project Docker with Node and MongoDB

This machine contains a node server and mongo database

Run it:

```
    npm install

    docker-compose up -d
```

Shutdown machine:

```
    docker-compose down
```